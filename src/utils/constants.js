const roles = ['student', 'mentor', 'counselor', 'admin'];

const apiMessages = {
  ready: 'Endpoint is ready',
  success: 'Request successful',
  error: 'Request failed',
};

module.exports = {
  roles,
  apiMessages,
};
